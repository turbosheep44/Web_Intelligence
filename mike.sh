python3 parsingStage.py #this is for the small one, will output a json and will populate a folder
python3 parsingStageAll.py #this is for the large one, will also output a json and will populate a folder
python3 stemmer.py #should take 2 arguments, input folder and output path
